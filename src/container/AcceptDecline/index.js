import React from 'react';
import AcceptDecline from '../../components/AcceptDecline';

const AcceptDeclineContainer = (props) => {
    return (<AcceptDecline {...props} />)
}

export default AcceptDeclineContainer